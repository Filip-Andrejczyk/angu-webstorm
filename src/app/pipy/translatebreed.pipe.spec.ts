import { TranslatebreedPipe } from './translatebreed.pipe';

describe('TranslatebreedPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatebreedPipe();
    expect(pipe).toBeTruthy();
  });
});
